[project]
name = "campus-agora-docs"
version = "0.1.0"
description = "Documentation tooling for Campus Agora."
requires-python = ">=3.12"
dependencies = [
  "mkdocs==1.6.1",
]

[tool.uv]
package = false
