# Agent Notes

Follow the repository spec in `docs/superpowers/specs/2026-07-06-campus-agora-init-design.md`.

Current implementation scope:

- M0 is a runnable repository skeleton.
- M0.1 adds contract and quality gates.
- M0.2 adds governance docs and risk boundaries.

Do not commit local reference files such as `ref*.md`, `temp*.md`, `api.md`, `backend.md`, `frontend.md`, or `monorepo.md` unless the user explicitly requests it.
